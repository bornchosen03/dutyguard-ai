# Root-level env example
# Put environment overrides in backend/.env and frontend/.env if needed
# DutyGuard root-level env example
# Copy this file to `.env` and fill values for local testing.

# Backend settings
DUTYGUARD_DATA_DIR=backend/data
DUTYGUARD_UPLOADS_DIR=backend/data/uploads

# Notification target (email the intake will be sent to)
# When empty, the app will write fallback entries to `backend/data/intake_notifications.jsonl`.
DUTYGUARD_NOTIFY_EMAIL_TO=

# SMTP settings (required to send real emails)
DUTYGUARD_SMTP_HOST=
DUTYGUARD_SMTP_PORT=587
DUTYGUARD_SMTP_USER=
DUTYGUARD_SMTP_PASSWORD=
DUTYGUARD_SMTP_USE_TLS=true

# Admin key for protected endpoints (set to a secret value)
DUTYGUARD_ADMIN_KEY=

# Optional: admin/reviewer email for other notifications
DUTYGUARD_NOTIFY_FROM="no-reply@example.com"

# Example: To test email locally, set DUTYGUARD_NOTIFY_EMAIL_TO and the SMTP_* vars above.
# Then run: `source .env && ./scripts/test_smtp.sh`
